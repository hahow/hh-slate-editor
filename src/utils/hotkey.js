import { isKeyHotkey } from 'is-hotkey';

export const isBoldHotkey = isKeyHotkey('mod+b');
export const isItalicHotkey = isKeyHotkey('mod+i');
export const isLinkHotKey = isKeyHotkey('mod+k');
export const isTab = isKeyHotkey('tab');
